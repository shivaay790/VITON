# Web API
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
pydantic==2.5.0
python-dotenv==1.0.0

# Virtual try-on (DM-VTON) and image handling.
# This installs the CPU build of PyTorch, which is enough: a try-on takes about
# a second. For an NVIDIA GPU install the CUDA build first, see the README.
torch==2.1.1
numpy==1.24.3
scipy==1.11.4
Pillow==10.1.0

# Semantic shop search. Optional at runtime: without a PINECONE_API_KEY the shop
# falls back to metadata filtering and FashionCLIP is never downloaded.
transformers==4.36.2
pinecone==8.1.0
